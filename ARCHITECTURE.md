# 🏗️ Architecture Diagrams

## System Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                       USER REQUEST                              │
│              "What's the weather in Paris?"                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                    ┌────────▼────────┐
                    │   HTTP Router   │
                    │  /chat/message  │
                    │   (Validates    │
                    │  w/ Pydantic)   │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │ Chat Service    │
                    │ process_message │
                    │  (Calls Ollama) │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │   Ollama LLM    │
                    │  (qwen2.5:7b)   │
                    │  (Returns tool  │
                    │   suggestion)   │
                    └────────┬────────┘
                             │
        ┌────────────────────┴────────────────────┐
        │ LLM says: Call "check_weather"          │
        │ with {"city": "Paris"}                  │
        └────────────────────┬────────────────────┘
                             │
                    ┌────────▼────────────┐
                    │  Tool Registry      │
                    │  execute_tool()     │
                    └────────┬────────────┘
                             │
        ┌────────────────────┴──────────────────────┐
        │ VALIDATION LAYER                         │
        │ ✓ Is "check_weather" whitelisted?        │
        │ ✓ Is {"city": "Paris"} valid?            │
        │ ✓ No URLs? No injection? No evil?        │
        │ ✓ Log the call to audit trail            │
        └────────────────────┬──────────────────────┘
                             │
        ┌────────────────────▼──────────────────────┐
        │ EXECUTION (Only if validation passed)    │
        │ Call weather API                         │
        │ Get result                               │
        │ Log result to audit trail                │
        └────────────────────┬──────────────────────┘
                             │
        ┌────────────────────▼──────────────────────┐
        │ RETURN RESPONSE                          │
        │ {"success": true,                        │
        │  "response": "It's 15°C in Paris...",    │
        │  "tool_results": [...]}                  │
        └──────────────────────────────────────────┘
```

---

## Validation Pipeline

```
┌──────────────────────┐
│   Tool Call Arrives  │
│ tool_name: "X"       │
│ tool_args: {...}     │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────┐
│ 1. Whitelist Check       │
│ Is X in ToolName enum?   │
└──────┬───────────────┬───┘
       │ YES           │ NO
       │               ▼
       │         ┌──────────────┐
       │         │ REJECT ❌    │
       │         │ Log security │
       │         │ event        │
       │         └──────────────┘
       ▼
┌──────────────────────────┐
│ 2. Argument Validation   │
│ Apply Pydantic schema    │
│ Run validators           │
└──────┬───────────────┬───┘
       │ PASS          │ FAIL
       │               ▼
       │         ┌──────────────┐
       │         │ REJECT ❌    │
       │         │ Log security │
       │         │ event        │
       │         └──────────────┘
       ▼
┌──────────────────────────┐
│ 3. Log Tool Call         │
│ timestamp, tool_name,    │
│ tool_args, user_message  │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ 4. Execute Tool          │
│ Call the function        │
└──────┬───────────────┬───┘
       │ SUCCESS       │ ERROR
       │               ▼
       │         ┌──────────────┐
       │         │ Log error ⚠️  │
       │         │ Return error │
       │         └──────────────┘
       ▼
┌──────────────────────────┐
│ 5. Log Result            │
│ execution_time           │
│ result_type              │
│ success flag             │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────┐
│ Return to LLM ✅         │
│ {"success": true,        │
│  "tool": "X",            │
│  "result": {...}}        │
└──────────────────────────┘
```

---

## Configuration Hierarchy

```
┌─────────────────────────────────┐
│     .env File                   │
│  (User's secrets)               │
│  OLLAMA_URL=...                 │
│  SPOTIFY_CLIENT_ID=...          │
│  OPENWEATHER_API_KEY=...        │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│  app/core/config.py             │
│  BaseSettings (Pydantic)        │
│  • ollama_url                   │
│  • ollama_model                 │
│  • spotify_client_id            │
│  • openweather_api_key          │
│  • log_tool_execution           │
│  • enable_tool_auditing         │
│  • max_requests_per_minute      │
└────────────┬────────────────────┘
             │
    ┌────────┴────────────────────────────────┐
    │        Used by all services             │
    │                                          │
    ▼                    ▼                     ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│chat_service  │  │weather_       │  │spotify_      │
│              │  │service        │  │service       │
│from app.core │  │from app.core  │  │from app.core │
│.config import│  │.config import │  │.config import│
│settings      │  │settings       │  │settings      │
└──────────────┘  └──────────────┘  └──────────────┘
```

---

## Tool Whitelist & Validation

```
┌─────────────────────────────────────────┐
│       TOOL WHITELIST (Enum)             │
│       ToolName                          │
├─────────────────────────────────────────┤
│ • PLAY_SONG                             │
│ • PAUSE_PLAYBACK                        │
│ • RESUME_PLAYBACK                       │
│ • NEXT_TRACK                            │
│ • PREVIOUS_TRACK                        │
│ • GET_CURRENT_TRACK                     │
│ • CHECK_WEATHER                         │
│ • GET_FORECAST                          │
│ • SEARCH_WIKI                           │
│ • GET_WIKI_SUMMARY                      │
│                                         │
│ ❌ Anything else is REJECTED            │
└─────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│    ARGUMENT SCHEMAS (Pydantic)          │
│                                         │
│ CheckWeatherArgs:                       │
│  - city: str (1-100 chars)              │
│    @validator → no URLs                 │
│    @validator → only letters/spaces     │
│                                         │
│ SearchWikiArgs:                         │
│  - query: str (1-500 chars)             │
│    @validator → no URLs                 │
│                                         │
│ PlaySongArgs:                           │
│  - song: str (1-500 chars)              │
│    @validator → no URLs                 │
│                                         │
│ ... (each tool has its schema)          │
└─────────────────────────────────────────┘
```

---

## Request/Response Flow

```
REQUEST
┌─────────────────────────────────┐
│ POST /chat/message              │
│ {                               │
│   "message": "...",             │
│   "history": [...]              │
│ }                               │
└────────────┬────────────────────┘
             │
    ┌────────▼────────┐
    │ Pydantic        │
    │ Validates       │
    │ ChatRequest     │
    └────────┬────────┘
             │
    ┌────────▼────────┐
    │ ChatService     │
    │ .process_       │
    │ message()       │
    └────────┬────────┘
             │
RESPONSE
┌────────▼────────────────────────┐
│ ChatResponse                     │
│ {                               │
│   "success": true,              │
│   "response": "...",            │
│   "tool_results": [             │
│     {                           │
│       "tool": "check_weather",  │
│       "result": {               │
│         "success": true,        │
│         "temperature": 15       │
│       }                         │
│     }                           │
│   ],                            │
│   "available_tools": [...]      │
│ }                               │
└────────────────────────────────┘
```

---

## Audit Trail

```
┌────────────────────────────────────────┐
│         AUDIT LOG FILE                 │
│       (JSON Line Format)               │
├────────────────────────────────────────┤
│                                        │
│ {"timestamp": "...", "event":          │
│  "tool_call", "tool": "check_weather"} │
│                                        │
│ {"timestamp": "...", "event":          │
│  "tool_result", "tool": "check_weather"│
│  , "success": true, ...}               │
│                                        │
│ {"timestamp": "...", "event":          │
│  "tool_rejected", "tool": "evil_tool"} │
│                                        │
│ {"timestamp": "...", "event":          │
│  "tool_execution_error", ...}          │
│                                        │
└────────────────────────────────────────┘
        │
        ▼
┌────────────────────────────────────────┐
│    MONITORING & ALERTING               │
│ (Future Enhancement)                   │
│                                        │
│ • Alert on tool rejections             │
│ • Alert on execution errors            │
│ • Track tool usage patterns            │
│ • Rate limiting enforcement            │
└────────────────────────────────────────┘
```

---

## Security Layers

```
┌─────────────────────────────────────┐
│ Layer 1: HTTP Input Validation      │
│ • Pydantic ChatRequest schema       │
│ • Type checking                     │
│ • Length validation                 │
└────────────┬────────────────────────┘
             │
┌────────────▼────────────────────────┐
│ Layer 2: Tool Call Validation       │
│ • Whitelist check (ToolName enum)   │
│ • Argument validation (Pydantic)    │
│ • Sanitization (regex validators)   │
└────────────┬────────────────────────┘
             │
┌────────────▼────────────────────────┐
│ Layer 3: Execution Logging          │
│ • Log before execution              │
│ • Log after execution               │
│ • Track execution time              │
└────────────┬────────────────────────┘
             │
┌────────────▼────────────────────────┐
│ Layer 4: Configuration Security     │
│ • Centralized in app/core/config.py │
│ • Type-safe with Pydantic           │
│ • No hardcoded secrets              │
└────────────┬────────────────────────┘
             │
┌────────────▼────────────────────────┐
│ Layer 5: HTTP Response Validation   │
│ • Pydantic ChatResponse schema      │
│ • Type checking on output           │
│ • Format validation                 │
└─────────────────────────────────────┘
```

---

## File Organization

```
app/
│
├── core/                          ← NEW
│   ├── config.py                  ← Settings
│   ├── security.py                ← Validators
│   └── logging.py                 ← Audit trail
│
├── routers/
│   └── chat_router.py             ← HTTP layer
│
├── services/
│   └── chat_service.py            ← Business logic
│
├── tools/
│   └── tool_registry.py           ← Tool execution
│
└── schemas/
    └── responses.py               ← Data contracts
```

**Each layer has ONE responsibility. Easy to test. Easy to audit. Easy to secure.**

---
